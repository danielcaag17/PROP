package src.domain.classes;

public class QAP implements Strategy {
    static float cotaGilmore() {
        // 1r terme => Calculable
        // sum(cotas) on:
        //      cota = [ 1 - (freq(i->j) + freq(j->j))/2] * dist(Pi, Pj)

        // 2n + 3r terme => Hungarian Algorithm
    }
    public void generarTeclat() {
        // asd    
    }
}
